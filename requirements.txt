# =============================================================================
# Conversational Sales Agent - Production Requirements
# =============================================================================
# NOTE: This file is for Docker/Cloud Run deployment.
# For local dev with eval/testing extras, install BootStrapAgent/bootstrap_agent/requirements.txt
# separately.

# --- Core Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Google AI / ADK (runtime only — no [eval] extra, saves ~2GB PyTorch/CUDA) ---
google-genai>=1.0.0
google-adk>=1.20.0

# --- Configuration & Environment ---
python-dotenv>=1.1.1
pydantic>=2.11.7

# --- Data Processing ---
pandas>=2.0.0

# --- Vector Database & RAG (ProductAgent) ---
chromadb>=0.5.0
sentence-transformers>=2.7.0

# --- Caching (PaymentAgent, ServiceFulfillmentAgent) ---
diskcache>=5.6.0

# --- Google Cloud Storage (GCS DB sync in entrypoint.sh) ---
google-cloud-storage>=2.14.0

# --- Utilities ---
requests>=2.32.5